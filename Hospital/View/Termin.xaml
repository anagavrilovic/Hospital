<Window x:Class="Hospital.View.Doctor_Examination"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hospital.View"
        mc:Ignorable="d"
        Title="Termin" WindowStartupLocation="CenterScreen"
        Style="{StaticResource CustomWindowStyle}">

    <DockPanel x:Name="LayoutRoot">
        <DockPanel DockPanel.Dock="Top">
            <StackPanel Orientation="Horizontal">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Margin" Value="250,0,0,5"/>
                    </Style>
                </StackPanel.Resources>
                <Button Name="image" Margin="40,20,20,20" Width="80" Height="50" Click="image_Click">
                    <Button.Content>
                        <Image Source="/Icon/logo.png" />
                    </Button.Content>
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </DockPanel>
        <DockPanel DockPanel.Dock="Bottom">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Grid.Row="0" Content="Anamneza" Height="94" Margin="99,15,101.8,14.2" Click="Button_Click_1"/>
                <Button x:Name="dijagnozaDijalog"  Grid.Column="0" Grid.Row="1" Content="Dijagnostika" Height="94" Margin="99,15,101.8,14.2" Click="Button_Click"/>
                <Button Grid.Column="0" Grid.Row="2" Content="Medicinski karton" Height="94" Margin="99,15,101.8,14.2"/>
                <Button Grid.Column="1" Grid.Row="0" Content="Termini" Height="94" Margin="99,15,101.8,14.2" Click="Button_Click_2"/>
                <Button Grid.Column="1" Grid.Row="1" Content="Modifikacija alergena" Height="94" Margin="99,15,101.8,14.2"/>
                <Button Grid.Column="1" Grid.Row="2" Content="Terapija" Height="94" Margin="99,15,101.8,14.2" Click="Terapija"/>
            </Grid>


        </DockPanel>
    </DockPanel>
</Window>
