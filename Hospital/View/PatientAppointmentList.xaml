<Window x:Class="Hospital.View.PatientAppointmentList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hospital.View"
        mc:Ignorable="d"
        Title="PatientAppointmentList" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="dataGridApp" ItemsSource="{Binding Lista}" IsReadOnly="True" 
                  SelectionMode="Single" AutoGenerateColumns="False" Margin="0,0,107,110">
            <DataGrid.Columns>
                <DataGridTextColumn Header="DatumVreme" Binding="{Binding DateTime}" Width="*"/>
                <DataGridTextColumn Header="Doktor" Binding="{Binding DoctrosNameSurname}" Width="*"/>
                <DataGridTextColumn Header="Tip" Binding="{Binding type}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Izmeni termin" HorizontalAlignment="Left" Height="34" Margin="427,348,0,0" VerticalAlignment="Top" Width="118"/>
        <Button Content="Otkaži termin" HorizontalAlignment="Left" Height="34" Margin="637,348,0,0" VerticalAlignment="Top" Width="118" Click="Button_Click"/>
    </Grid>
</Window>
