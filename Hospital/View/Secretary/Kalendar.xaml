<Page x:Class="Hospital.View.Kalendar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hospital.View"
        mc:Ignorable="d"
        Title="Kalendar" d:DesignHeight="700" d:DesignWidth="1065" 
        Background="{StaticResource BackgroundGray}">
    <Page.Resources>
        <ResourceDictionary Source="../../Resources/SecretaryResourceDictionary.xaml"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="RASPORED PREGLEDA I OPERACIJA" Style="{StaticResource TitleStyle}"/>
            <!--<Separator Margin="15,0"/>-->
        </StackPanel>

        <Border Grid.Row="1" Margin="15" CornerRadius="10" Background="{StaticResource MainWhite}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid Margin="0,10,0,5" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Doctor ComboBox -->
                    <ComboBox x:Name="DoctorComboBox" Width="160" Height="25" Margin="5" Style="{StaticResource cbTB}"
                              ItemsSource="{Binding Doctors}" SelectedItem="{Binding SelectedDoctor}" SelectionChanged="ComboBoxDoctorEvent"/>

                    <!-- Prozor za odabir pacijenta -->
                    <Button x:Name="Pacijent" Grid.Column="1" Content="Odaberi pacijenta" Margin="5" Width="120" Height="25" 
                            Style="{StaticResource ButtonCalendar}" Click="OdaberiPacijentaClick"/>

                    <!-- Dugmad za zakazivanje i otkazivanje pregleda -->
                    <Button x:Name="Zakazi" Grid.Column="2" Height="25" Width="25" Margin="5" Style="{StaticResource ButtonCalendar}" Click="ZakaziClick">
                        <Image Source="../../Icon/Secretary/plusdate.png" Width="15" Height="25"/>
                    </Button>
                    <Button x:Name="Otkazi" Grid.Column="3" Height="25" Width="25" Margin="5" Style="{StaticResource ButtonCalendar}" Click="OtkaziClick">
                        <Image Source="../../Icon/Secretary/minusdate.png" Width="15" Height="25"/>
                    </Button>

                    <!-- Odabir datuma prikaza  -->
                    <DatePicker x:Name="Datum" Grid.Column="6" Focusable="False" Width="30" Height="25" Margin="5" 
                                SelectedDate="{Binding Date}" FirstDayOfWeek="Monday" SelectedDateChanged="DateChanged"/>
                    <Button x:Name="TrenutnaNedelja" Grid.Column="7" Height="25" Width="110" Content="Trenutna nedelja" Margin="5" 
                            Style="{StaticResource ButtonCalendar}" Click="TrenutnaNedeljaClick" />

                    <!-- Previous week -->
                    <Button x:Name="Previous" Grid.Column="8" Width="25" Height="25" Margin="5" Style="{StaticResource ButtonCalendar}" Click="PreviousClick">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="25"/>
                            </Style>
                        </Button.Resources>
                        <Image Source="../../Icon/Secretary/1.png" Width="15" Height="25"/>
                    </Button>

                    <!-- Next week -->
                    <Button x:Name="Next" Grid.Column="9" Width="25" Height="25" Margin="5" Style="{StaticResource ButtonCalendar}" Click="NextClick">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="25"/>
                            </Style>
                        </Button.Resources>
                        <Image Source="../../Icon/Secretary/11.png" Width="15" Height="25"/>
                    </Button>

                </Grid>


                <!-- Scheduler -->
                <DataGrid x:Name="KalendarDataGrid" Grid.Row="1" ItemsSource="{Binding Tabela, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Style="{StaticResource Kalendar}" Margin="0,0,0,10" >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="" Width="40" Binding="{Binding Vreme}" HeaderStyle="{StaticResource CenterGridHeaderStyle}"/>

                        <DataGridTextColumn Header="Ponedeljak" Width="1*" Binding="{Binding Ponedeljak}" HeaderStyle="{StaticResource CenterGridHeaderStyle}">
                            <DataGridTextColumn.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Ponedeljak.Type}" Value="0">
                                            <Setter Property="Background" Value="#FFDCE594"/>
                                            <Setter Property="ToolTip" Value="Pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Ponedeljak.Type}" Value="1">
                                            <Setter Property="Background" Value="#FFA3B9CD"/>
                                            <Setter Property="ToolTip" Value="Operacija"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Ponedeljak.Type}" Value="2">
                                            <Setter Property="Background" Value="#FFFF9494"/>
                                            <Setter Property="ToolTip" Value="Hitan pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Ponedeljak.Type}" Value="3">
                                            <Setter Property="Background" Value="#FFFF5C5C"/>
                                            <Setter Property="ToolTip" Value="Hitna operacija"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Utorak" Width="1*" Binding="{Binding Utorak}" HeaderStyle="{StaticResource CenterGridHeaderStyle}">
                            <DataGridTextColumn.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Utorak.Type}" Value="0">
                                            <Setter Property="Background" Value="#FFDCE594"/>
                                            <Setter Property="ToolTip" Value="Pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Utorak.Type}" Value="1">
                                            <Setter Property="Background" Value="#FFA3B9CD"/>
                                            <Setter Property="ToolTip" Value="Operacija"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Utorak.Type}" Value="2">
                                            <Setter Property="Background" Value="#FFFF9494"/>
                                            <Setter Property="ToolTip" Value="Hitan pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Utorak.Type}" Value="3">
                                            <Setter Property="Background" Value="#FFFF5C5C"/>
                                            <Setter Property="ToolTip" Value="Hitna operacija"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Sreda" Width="1*" Binding="{Binding Sreda}" HeaderStyle="{StaticResource CenterGridHeaderStyle}">
                            <DataGridTextColumn.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Sreda.Type}" Value="0">
                                            <Setter Property="Background" Value="#FFDCE594"/>
                                            <Setter Property="ToolTip" Value="Pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Sreda.Type}" Value="1">
                                            <Setter Property="Background" Value="#FFA3B9CD"/>
                                            <Setter Property="ToolTip" Value="Operacija"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Sreda.Type}" Value="2">
                                            <Setter Property="Background" Value="#FFFF9494"/>
                                            <Setter Property="ToolTip" Value="Hitan pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Sreda.Type}" Value="3">
                                            <Setter Property="Background" Value="#FFFF5C5C"/>
                                            <Setter Property="ToolTip" Value="Hitna operacija"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Cetvrtak" Width="1*" Binding="{Binding Cetvrtak}" HeaderStyle="{StaticResource CenterGridHeaderStyle}">
                            <DataGridTextColumn.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Cetvrtak.Type}" Value="0">
                                            <Setter Property="Background" Value="#FFDCE594"/>
                                            <Setter Property="ToolTip" Value="Pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Cetvrtak.Type}" Value="1">
                                            <Setter Property="Background" Value="#FFA3B9CD"/>
                                            <Setter Property="ToolTip" Value="Operacija"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Cetvrtak.Type}" Value="2">
                                            <Setter Property="Background" Value="#FFFF9494"/>
                                            <Setter Property="ToolTip" Value="Hitan pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Cetvrtak.Type}" Value="3">
                                            <Setter Property="Background" Value="#FFFF5C5C"/>
                                            <Setter Property="ToolTip" Value="Hitna operacija"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Petak" Width="1*" Binding="{Binding Petak}" HeaderStyle="{StaticResource CenterGridHeaderStyle}">
                            <DataGridTextColumn.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Petak.Type}" Value="0">
                                            <Setter Property="Background" Value="#FFDCE594"/>
                                            <Setter Property="ToolTip" Value="Pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Petak.Type}" Value="1">
                                            <Setter Property="Background" Value="#FFA3B9CD"/>
                                            <Setter Property="ToolTip" Value="Operacija"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Petak.Type}" Value="2">
                                            <Setter Property="Background" Value="#FFFF9494"/>
                                            <Setter Property="ToolTip" Value="Hitan pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Petak.Type}" Value="3">
                                            <Setter Property="Background" Value="#FFFF5C5C"/>
                                            <Setter Property="ToolTip" Value="Hitna operacija"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Subota" Width="1*" Binding="{Binding Subota}" HeaderStyle="{StaticResource CenterGridHeaderStyle}">
                            <DataGridTextColumn.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Subota.Type}" Value="0">
                                            <Setter Property="Background" Value="#FFDCE594"/>
                                            <Setter Property="ToolTip" Value="Pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Subota.Type}" Value="1">
                                            <Setter Property="Background" Value="#FFA3B9CD"/>
                                            <Setter Property="ToolTip" Value="Operacija"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Subota.Type}" Value="2">
                                            <Setter Property="Background" Value="#FFFF9494"/>
                                            <Setter Property="ToolTip" Value="Hitan pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Subota.Type}" Value="3">
                                            <Setter Property="Background" Value="#FFFF5C5C"/>
                                            <Setter Property="ToolTip" Value="Hitna operacija"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Nedelja" Width="1*" Binding="{Binding Nedelja}" HeaderStyle="{StaticResource CenterGridHeaderStyle}">
                            <DataGridTextColumn.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Nedelja.Type}" Value="0">
                                            <Setter Property="Background" Value="#FFDCE594"/>
                                            <Setter Property="ToolTip" Value="Pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Nedelja.Type}" Value="1">
                                            <Setter Property="Background" Value="#FFA3B9CD"/>
                                            <Setter Property="ToolTip" Value="Operacija"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Nedelja.Type}" Value="2">
                                            <Setter Property="Background" Value="#FFFF9494"/>
                                            <Setter Property="ToolTip" Value="Hitan pregled"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Nedelja.Type}" Value="3">
                                            <Setter Property="Background" Value="#FFFF5C5C"/>
                                            <Setter Property="ToolTip" Value="Hitna operacija"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>

                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>
