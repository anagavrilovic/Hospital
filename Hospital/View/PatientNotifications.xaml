<Window x:Class="Hospital.View.PatientNotifications"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hospital.View"
        mc:Ignorable="d"
        Title="PatientNotifications" Height="450" Width="800"
        KeyDown="myTestKey">
    <Grid>
        <DataGrid x:Name="dataGridApp" ItemsSource="{Binding Lista}" IsReadOnly="True" 
                  SelectionMode="Single" AutoGenerateColumns="False" Margin="0,0,107,110">
            <DataGrid.Columns>
                <DataGridTextColumn  Header="Naziv" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn  Header="Vreme" Binding="{Binding Times}" Width="*"/>
                <DataGridTextColumn  Header="Trajanje" Binding="{Binding Duration}" Width="*"/>
                <DataGridTextColumn  Header="Pročitano" Binding="{Binding Read}" Width="*"/>
                <DataGridTextColumn  Header="Aktivan" Binding="{Binding Active}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
