<Window x:Class="Hospital.View.Anamnesis"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hospital.View"
        mc:Ignorable="d"
        Title="Anamnesis" Height="450" Width="450" 
        WindowStartupLocation="CenterOwner">
    <DockPanel>
        <DockPanel DockPanel.Dock="Bottom">
            <Button HorizontalAlignment="Left" Height="30" Width="50" Margin="20,15,0,20" Click="Button_Click_1">Save</Button>
            <Button HorizontalAlignment="Right" Height="30" Width="50" Margin="0,15,20,20" Click="Button_Click_2">Cancel</Button>
        </DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <ScrollViewer CanContentScroll="True">
                <TextBox Name="anamnesisBox" TextWrapping="Wrap" AcceptsReturn="True" FontSize="13" Text="{Binding Path=Test1,Mode=TwoWay,UpdateSourceTrigger=Explicit}"></TextBox>
            </ScrollViewer>
        </DockPanel>
    </DockPanel>
</Window>
