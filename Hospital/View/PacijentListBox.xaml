<Window x:Class="Hospital.View.PacijentListBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hospital.View"
        mc:Ignorable="d"
        Title="PacijentListBox" Height="450" Width="450">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <WrapPanel>
                <TextBox x:Name="pretrazi" Margin="40,20,20,20" Height="30" Width="230" BorderThickness="1"></TextBox>
                <Button Name="image" Margin="0,20,20,20" Width="60" Height="30" Click="filterPatients">
                    <Button.Content>
                        <Image Source="/Icon/search.png" />
                    </Button.Content>
                </Button>
            </WrapPanel>
        </DockPanel>
        <ListBox Name="listBox" Margin="40,40,40,40" MouseDoubleClick="listBox_MouseDoubleClick"></ListBox>

    </DockPanel>
</Window>
