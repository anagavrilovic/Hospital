<Window x:Class="Hospital.View.RoomsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hospital.View"
        mc:Ignorable="d"
         WindowStartupLocation="CenterOwner"
        Title="Pregled Sala" Height="400" Width="650">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>


        <DataGrid x:Name="dataGridRooms" ItemsSource="{Binding Path=Rooms, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" 
                  SelectionMode="Single" AutoGenerateColumns="False" HorizontalAlignment="Left" 
                  Height="196" Margin="88,98,0,0" VerticalAlignment="Top" Width="475" 
                  Background="#FFE3F4F1" FontFamily="Arial" Foreground="#FF536AA0" Grid.RowSpan="2">
            
            <DataGrid.Columns>
                <DataGridTextColumn     Header="Oznaka"                Binding="{Binding Id}"/>
                <DataGridTextColumn     Header="Naziv"    Width="*"    Binding="{Binding Name}" />
                <DataGridTextColumn     Header="Sprat"    Width="*"    Binding="{Binding Floor}"/>
                <DataGridCheckBoxColumn Header="Slobodna" Width="*"    Binding="{Binding IsAvaliable}"/>
                <DataGridTextColumn     Header="Tip"      Width="1.3*" Binding="{Binding Type}"/>
            </DataGrid.Columns>
            
        </DataGrid>

        <Button x:Name="add"    Content="Dodaj"    HorizontalAlignment="Left"   Height="29"  Margin="88,33,0,0"    VerticalAlignment="Top" Width="79" Background="#FFD2F4EE" Foreground="#FF1E49D9" Click="addRoom"/>
        <Button x:Name="edit"   Content="Izmeni"   HorizontalAlignment="Left"   Height="29"  Margin="183,33,0,0"   VerticalAlignment="Top" Width="84" Background="#FFD2F4EE" Foreground="#FF1E49D9" Click="editRoom"/>
        <Button x:Name="delete" Content="Izbriši"  HorizontalAlignment="Right"  Height="29"  Margin="0,33,269.2,0" VerticalAlignment="Top" Width="79" Background="#FFD2F4EE" Foreground="#FF1E49D9" Click="deleteRoom"/>
                          
        <Button x:Name="staticInventory" Content="Statički inventar" Height="29" 
                Background="#FFD2F4EE"  Foreground="#FF1E49D9" Margin="401,33,129.2,202.8" Click="viewStaticInventory"/>


    </Grid>

</Window>
